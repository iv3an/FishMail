# Samples

Use fictional `.eml` messages to practice parsing and analysis. Email files are ignored by Git by default to avoid accidentally publishing real messages.

When we create a reviewed, fictional fixture for public tests, we can add a narrow exception for that specific file.
