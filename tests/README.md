# Tests

Add tests as analysis functions are implemented. Start with missing files, incorrect extensions, and sender/subject extraction from fictional email fixtures.

Python's built-in `unittest` is enough to begin. No tests are implemented yet.
