# Integrations

Later, add `gmail_client.py` for Gmail authentication and message retrieval, and `virustotal_client.py` for reputation lookups. Keep external API logic here so `main.py` stays focused on the menu.

No integrations are implemented yet. Do not put credentials or tokens in this folder.
